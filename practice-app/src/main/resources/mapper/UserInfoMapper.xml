<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reservation.mapper.UserInfoMapper">
	
    <select id="getUser" parameterType="string" resultType="com.reservation.entity.UserInfo">
    	SELECT * FROM user_info WHERE user_id = #{userId}
    </select>
	
    <insert id="register" parameterType="com.reservation.entity.UserInfo">
    </insert>
    
    <update id="" parameterType="">
    </update>
	
    <delete id="" parameterType="">
    </delete>

</mapper>